// Importing the schema types from about.js and brand.js
import about from './about'
import brand from './brand'

// Creating an array of schema types
const schemaTypes = [about, brand]
         
// Exporting the array of schema types
export {schemaTypes}
